import { SearchFormControl } from './search-form-control';

describe('SearchFormControl', () => {
  it('should create an instance', () => {
    expect(new SearchFormControl()).toBeTruthy();
  });
});
